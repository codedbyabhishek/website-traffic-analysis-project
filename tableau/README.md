# Tableau Usage

1. Import `kpi/channel_performance.csv` and `data/processed/clean_traffic.csv` into Tableau.
2. Build dashboards for:
   - Hourly session trend
   - Channel conversion comparison
   - Device-level engagement split
3. Publish dashboard to Tableau Server/Public for stakeholder sharing.
